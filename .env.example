# Backend config
PORT=8787

# Only allow your site to call the API (comma-separated). Example:
# ALLOWED_ORIGINS=https://amensis-game.com,https://www.amensis-game.com,http://localhost:5500
ALLOWED_ORIGINS=http://localhost:5500

# SMTP settings
#
# ✅ Brevo SMTP relay (recommended for this backend)
# Host: smtp-relay.brevo.com
# Ports: 587 or 2525 (no encryption) / 465 (SSL/TLS)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=false

# Brevo SMTP credentials (Settings → SMTP & API keys → SMTP tab)
# IMPORTANT: for SMTP relay, use an SMTP key / master password (NOT the API key).
SMTP_USER=SEU_SMTP_LOGIN_DA_BREVO
SMTP_PASS=SUA_SMTP_KEY_AQUI

# (Optional alternative) Brevo Transactional Email API key (only needed if you switch from SMTP to API)

# Mail routing
MAIL_TO=press@amensis-game.com
MAIL_FROM=no-reply@amensis-game.com
# Optional: display name in the From header
MAIL_FROM_NAME=Amensis Website
